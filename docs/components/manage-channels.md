# Manage Channels

A Livewire table component for managing alert channels.

## Usage

```blade
@livewire('alert-system.manage-channels')
```

Route: `GET /admin/alerts/channels` (name: `alerts.channels`)

## Features

- Searchable and sortable table of alert channels
- Edit channel name via modal
- Bulk export (all, filtered, selected) to Excel
