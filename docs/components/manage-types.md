# Manage Types

A Livewire table component for managing alert types.

## Usage

```blade
@livewire('alert-system.manage-types')
```

Route: `GET /admin/alerts/types` (name: `alerts.types`)

## Features

- Searchable and sortable table of alert types
- Edit type name via modal
- Bulk export (all, filtered, selected) to Excel
